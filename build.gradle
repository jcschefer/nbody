apply plugin: 'java'

sourceSets {
	main {
		java {
			srcDirs = ["src"]
		}
	}
}

jar {
   manifest {
      attributes(
         'Main-Class': 'nbody.main.Simulation',
			'Class-Path': configurations.compile.files.collect { it.gettName() }.join(' ')
		)
   }
}
